name: analog_clock_picker
description: A customable time picker using analog clock format
version: 0.0.6
homepage: https://github.com/yasfdany/analog_clock_picker

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
